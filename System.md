# GenieAI System Architecture & Specification Document

GenieAI คือระบบผู้ช่วยธุรกิจอัจฉริยะแบบ Multi-tenant SaaS สำหรับผู้ประกอบการ (เช่น ร้านสปา ร้านทำผม คลินิก หรือร้านอาหาร) โดยระบบจะรับคู่มือการให้บริการ (PDF/TXT/MD) มาประมวลผลเพื่อสร้าง AI Chatbot ตอบคำถามลูกค้าและทำรายการจองนัดหมายโดยอัตโนมัติผ่าน LINE OA รวมถึงช่องทางแชตอื่นๆ พร้อมระบบจัดการคิวนัดหมายและ Live Chat สำหรับแอดมินผู้ควบคุมระบบ

---

## 1. System Overview (ภาพรวมระบบ)

ระบบในเวอร์ชันปัจจุบันถูกพัฒนาขึ้นมาเพื่อเป็น **Single-tenant End-to-End Prototype** ที่ใช้งานได้จริงผ่าน LINE OA โดยมีสถาปัตยกรรมแยกเป็น Backend และ Frontend ดังนี้:

* **Backend**: ขับเคลื่อนด้วย FastAPI (Python 3.11) ทำหน้าที่ประมวลผลข้อมูลเอกสารด้วย OCR, จัดการเวกเตอร์ฐานข้อมูล (ChromaDB), บริหารประวัติแชต (Redis) และเชื่อมต่อกับ OpenAI (GPT-4o-mini)
* **Frontend**: พัฒนาด้วย React 19, Vite 8, HeroUI Component Library และ Tailwind CSS v4 ดีไซน์สวยงามระดับพรีเมียมด้วยธีม Glassmorphism
* **Database & Cache**: 
  * ใช้ **Local JSON files** (`data/`) เป็นตัวเก็บข้อมูลถาวรสำหรับการตั้งค่าโปรไฟล์ร้านค้า การจอง และผู้ใช้ (เตรียมการเปลี่ยนไปใช้ MongoDB ในอนาคต)
  * ใช้ **Redis** สำหรับการเก็บข้อมูลประวัติการสนทนาชั่วคราวและแคชโปรไฟล์ร้านค้า
  * ใช้ **ChromaDB (Embedded)** สำหรับเก็บข้อความเวกเตอร์จากเอกสารคู่มือ

---

## 2. Directory Structure (โครงสร้างไดเรกทอรี)

```text
/ (Project Root)
├── backend/
│   ├── app/
│   │   ├── core/              # การตั้งค่าระบบ และการเชื่อมต่อฐานข้อมูล
│   │   │   ├── config.py
│   │   │   └── redis_service.py # บริการจัดการคิวและประวัติสนทนาใน Redis
│   │   ├── models/            # โมเดลข้อมูล Pydantic สำหรับคัดกรองข้อมูล
│   │   ├── routers/           # เส้นทาง API (Endpoints)
│   │   │   ├── auth.py        # การล็อกอินและการ onboarding ของผู้ใช้
│   │   │   ├── bookings.py    # รายการแสดงและยกเลิกนัดหมาย
│   │   │   ├── chat.py        # แซนด์บ็อกซ์สำหรับทดสอบการสนทนาของ AI
│   │   │   ├── documents.py   # จัดการอัปโหลดและประมวลผลคู่มือ
│   │   │   ├── tenant.py      # จัดการข้อมูลร้านค้า (Profile)
│   │   │   └── webhooks.py    # LINE webhook รับส่งข้อความกับบอต
│   │   ├── services/          # ตรรกะการประมวลผล RAG/CAG และฟังก์ชันจอง
│   │   │   ├── booking_service.py # ตรวจเช็คเวลาชนและเขียนไฟล์จอง
│   │   │   ├── openai_service.py  # บริการคุยกับ GPT และรันเครื่องมืออัตโนมัติ
│   │   │   ├── parsing.py         # ตัวแยกเอกสาร PDF ด้วย Vision OCR / PyMuPDF
│   │   │   └── rag.py             # ระบบไฮบริดค้นข้อมูล RAG+CAG
│   │   └── main.py            # จุดเริ่มต้นแอปพลิเคชัน FastAPI
│   ├── requirements.txt
│   └── .env
│
├── frontend/
│   ├── src/
│   │   ├── components/        # คอมโพเนนต์ UI นำกลับมาใช้ซ้ำ (Sidebar, GlowEffect)
│   │   ├── pages/             # หน้าการแสดงผลหลักของแอปพลิเคชัน
│   │   │   ├── AuthPage.jsx          # หน้าลงทะเบียนและเข้าสู่ระบบ
│   │   │   ├── BookingsManager.jsx   # ปฏิทินแสดงรายการนัดหมายและจัดตาราง
│   │   │   ├── ChatSandbox.jsx       # แซนด์บ็อกซ์สำหรับทดสอบการคุยและดูรายละเอียด AI
│   │   │   ├── DashboardOverview.jsx # ภาพรวมสถิติ ข้อมูล และคิววันนี้
│   │   │   ├── DocumentManager.jsx   # จัดการข้อมูลพนักงาน บริการ โปรโมชัน และคู่มือ
│   │   │   └── OnboardingUpload.jsx  # ขั้นตอนแนะนำผู้ใช้งานให้อัปโหลดคู่มือ
│   │   ├── index.css          # ไฟล์กำหนดสไตล์ CSS และตัวแปรสี Brand
│   │   ├── App.jsx            # จุดเชื่อมต่อการเปลี่ยนหน้าจอหลักแบบ Tab-based
│   │   └── main.jsx
│   ├── package.json
│   ├── vite.config.js
│   └── .env
│
├── docs/                      # เอกสารอธิบายระบบ สถาปัตยกรรม และ RISEN Log
│   ├── architecture.md
│   ├── risen_interpretation_log.md
│   └── sample_shop_rules.md
└── System.md                  # เอกสารสรุปการทำโครงสร้างระบบ (ฉบับนี้)
```

---

## 3. Data Flow: Customer Message to AI Response
(ขั้นตอนการประมวลผลข้อความจากผู้ใช้สู่การตอบกลับ)

1. **Webhook Event Received**: เมื่อลูกค้าทักแชต LINE OA ข้อความจะถูกส่งเข้า Webhook `/api/webhooks/line` ➡️ ทำการคัดแยกข้อความและเรียกทำงานแบบเบื้องหลัง (BackgroundTasks)
2. **Retrieve Context (RAG/CAG Engine)**: 
   * ระบบเช็คขนาดข้อมูลทั้งหมดของร้านค้า (`CAG_TOKEN_THRESHOLD` = 15,000 Tokens)
   * **CAG Mode (คลังข้อมูลเล็ก)**: นำเอกสารทั้งหมดใส่ลงใน Prompt ตรงๆ
   * **RAG Mode (คลังข้อมูลใหญ่)**: ค้นหาความคล้ายคลึงเวกเตอร์ใน ChromaDB และดึงข้อมูล 5 ส่วนที่ใกล้เคียงที่สุดมาประกบกับ Prompt
3. **Session Fetch**: ดึงประวัติการคุยล่าสุดสูงสุด 10 ข้อความ จาก Redis แคช
4. **LLM Reasoning & Tool Calling**: ส่งข้อความพร้อมพารามิเตอร์ให้ GPT-4o-mini พร้อมเปิดฟังก์ชันการทำงานด้านการจอง:
   * **check_booking_availability**: ค้นหาเวลาว่างของพนักงานในระบบ
   * **create_booking**: บันทึกการจองลงในไฟล์ระบบหากไม่เกิดการชนกัน
5. **Split to Multi-bubble**: ข้อความตอบกลับที่ได้จะถูกตัดแต่งด้วยฟังก์ชัน `split_to_line_bubbles()` โดยอาศัยสัญลักษณ์ `---`
6. **Bilingual & Push Queue**: ส่งข้อความบับเบิลแรกกลับทันทีด้วย Reply Token (เพื่อประหยัดโควตา) และข้อความที่เหลือจะถูกทยอยส่งด้วย Push API โดยเว้นระยะ 2-4 วินาทีเพื่อจำลองการพิมพ์แบบมนุษย์

---

## 4. Technical Constraints & Constants (ตัวเลขและค่าคงที่ที่สำคัญ)

| ค่าคงที่ทางเทคนิค | ค่าตัวเลขที่กำหนด | รายละเอียดและการนำไปใช้งาน |
|---|---|---|
| **CAG Token Threshold** | **15,000 Tokens** | จุดวัดเพื่อสลับการใช้ RAG (เมื่อเอกสารใหญ่) หรือ CAG (เมื่อเอกสารเล็ก) |
| **Max History Messages**| **10 Messages** | จำนวนประวัติการพูดคุยที่บันทึกไว้ใน Redis เพื่อป้อนเป็นบริบทล่าสุด |
| **History TTL** | **2 Hours (7200s)**| ระยะเวลาหมดอายุของประวัติการคุยใน Redis นับจากข้อความล่าสุด |
| **Profile Cache TTL** | **10 Minutes (600s)**| ระยะเวลาที่ระบบบันทึกโปรไฟล์ร้านค้าลงใน Redis เพื่อประหยัดการอ่าน Disk |
| **Booking Slot Conflict**| **30 Minutes** | ระยะเวลาห้ามจองคิวซ้อนทับกันสำหรับพนักงานแต่ละคน |
| **Max LINE Bubbles** | **5 Bubbles** | จำนวนข้อความฟองแชตสูงสุดที่ระบบแยกส่งต่อ 1 คำถาม |
| **Human Typing Delay** | **2 - 4 Seconds** | จำลองช่วงเวลารอคอยก่อนพุชข้อความถัดไปให้ดูเสมือนคนจริงพิมพ์คุย |
| **Backend API Port** | **8000** | พอร์ตในการทำงานและติดต่อบริการจากหลังบ้าน |
| **Frontend Dev Port** | **5173** | พอร์ตสำหรับเรียกดูเว็บ Dashboard ผ่านเครื่องผู้ใช้งาน |

---

## 5. Security & Authentication (การรักษาความปลอดภัย)

* **ความปลอดภัยของ LINE**: ระบบคำนวณลายเซ็นข้อความด้วย HMAC-SHA256 โดยใช้ `Channel Secret` ตรวจสอบเปรียบเทียบกับค่า `X-Line-Signature` ทุกครั้งที่มีการส่งข้อมูลเข้ามา
* **ความปลอดภัยของฐานข้อมูล**: การเขียนไฟล์ JSON ของระบบทั้งหมดถูกปิดกั้นด้วย `threading.Lock()` เพื่อให้การประมวลผลแบบมัลติเธรดของ FastAPI ไม่เกิดปัญหาการเขียนทับซ้อนกัน (Race Condition)
* **การล็อกอิน (Auth)**: ในรุ่นต้นแบบนี้ระบบใช้การจับคู่ความถูกต้องจากอีเมลและเบอร์โทรศัพท์ (มีแผนขยายเป็น JWT และเข้ารหัสรหัสผ่านด้วย bcrypt ในเฟสถัดไป)
