{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//": "Replace YOUR-BACKEND with your real Render service name. This proxies API + static calls to the Render backend so the browser sees them as same-origin (the Bearer-token fetch wrapper keeps working, and there are no CORS issues).",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://YOUR-BACKEND.onrender.com/api/:path*" },
    { "source": "/static/:path*", "destination": "https://YOUR-BACKEND.onrender.com/static/:path*" }
  ]
}
